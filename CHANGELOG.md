# 変更履歴

版数の付け方は次のとおり。

```text
0.y.z   互換性を保証しない段階。y の更新で機能の追加、z の更新で修正
1.0.0   実運用での利用を想定できると判断した時点で付ける
```

## 未公開

### 追加

- 状態を変える入口へ、監査へ記録するか・しないかの宣言を義務づけた。
  記録しない側は理由を書く。宣言の無い入口は検査で落とす。
  記録の無い入口と、記録すべきなのに書き忘れた入口を区別できるようにした
- 申請種別の登録と変更を監査へ残すようにした。承認の経路そのものを決める
  ため、誰が何を承認できるかが変わる
- 外部認証で受け取る属性の対応を文書へ書き、契約のテストで固定した
- 取り込んでいるものの一覧（部品表）を書き出せるようにした。
  `bin/rails officeweave:sbom` で CycloneDX 形式の JSON を出力する。
  脆弱性の報告を受けたときに、自分の環境が対象かどうかを確かめられる。
  公開する版へ添える
- 画面の応答へ Content-Security-Policy を付けた。既定の出所を自分だけに限り、
  スクリプトの実行、枠への埋め込み、他の宛先への送信を許さない。この製品は
  ブラウザーへスクリプトを配信しないため、`script-src 'none'` とする。
  API の応答には付けない
- 検索できる対象を広げた。お知らせと申請を題名・本文で検索できる。
  文書は添付ファイルの名前でも検索できる。いずれも部分一致で、
  参照できる記録だけを対象にする
- API の一覧をページングするようにした。`page` と `per_page` で指定し、
  既定は 25 件、上限は 100 件である。応答へ `meta`（現在のページ、1 ページの
  件数、総数、総ページ数）を含める。これまで全件を返していた経路の応答が
  1 ページ分に変わる
- API に token ごとのレート制限を設けた。1 分あたり 300 要求までとし、
  超えた要求へは 429 と `rate_limited` を返す
- 一覧に絞り込みを足した。利用者は名前・メールアドレス、部門、状態で
  絞れる。通知は未読だけを表示できる。申請は種別で絞れる。絞り込みの値は
  ページを送っても引き継ぐ
- 一覧にページ送りを足した。利用者、文書、申請、通知、監査記録の一覧が、
  1 ページ 25 件（監査記録は 50 件）ずつ表示になる。絞り込みの値は
  ページを送っても引き継ぐ
- 予定を繰り返して作れるようにした。毎日、毎週、毎月から間隔を選び、
  終わりの日を指定する。1 度に作れるのは 60 回までである。各回は独立した
  予定として作られ、1 回だけ直すことも、その回以降をまとめて消すこともできる
- 予定へ参加者を指名できるようにした。指名した利用者は、公開範囲に関わらず
  その予定を見られる。指名したときに通知が届き、その通知は配信設定で
  切り替えられる。出欠の返答は扱わない
- 予約を変更できるようにした。時間帯、設備、用途、結び付ける予定を、
  取り消して作り直さずに直せる。変更できるのは予約した利用者と管理者で、
  取り消しと同じ範囲である。重なる時間帯へは変更できない
- 承認の委任を用意した。不在のあいだ、自分が担当する承認を別の利用者へ
  任せられる。設定の画面から本人が登録し、期間は日の単位で指定する。
  担当そのものは移らず、決裁できる範囲が広がるだけである。代理での決裁は、
  誰の代わりかを履歴へ残す。自分の申請は、委任を受けていても決裁できない
- 申請の承認経路を、提出の時点で写して残すようにした。あとから種別の段を
  変えても、提出済みの申請が通る経路は変わらない。申請の画面には、経路と、
  承認した段の承認者と時刻が出る。この版より前に提出された申請には写した
  経路が無く、その場合は種別の段を経路として表示する
- 申請の承認を多段にできるようにした。種別ごとに承認の段を並べ、上から順に
  承認する。途中の段の承認では承認済みにならず、最後の段の承認で承認済みに
  なる。差し戻しはどの段からでも申請者へ戻し、再提出は 1 段目から始まる。
  これまでの「承認する部門」は 1 段目として引き継ぐため、1 段だけの種別の
  動きは変わらない。進行中の申請は、段の構成を変えても止まらない
- 部門を CSV で取り込めるようにした。識別子が一致する部門は更新し、なければ
  追加する。上位部門は識別子で結び付け、行の並びは問わない。1 件でも誤りが
  あると何も取り込まれない。書き出したものをそのまま戻せる。部門の書き出しも
  監査記録へ残るようにした
- OIDC でログインできるようにした。`AUTHENTICATION_PROVIDER=oidc` と
  発行者、client_id、client_secret を設定すると、ログイン画面から認可
  サーバーへ送り出す。戻り先は `/oidc/callback` である。利用者は自動で
  作らず、メールアドレスで既にいる利用者を探す。この方式では、パスワードの
  入力欄、変更、再設定の経路を出さない。設定しない場合は内部認証のままで
  何も変わらない
- 外部接続用の token に読める範囲を設けられるようにした。発行のときに
  お知らせ、予定、部門、利用者から必要なものを選ぶ。範囲の外へ要求すると
  403 を返す。範囲は発行した利用者の権限を狭めるだけで、広げない。
  この版より前に発行した token は範囲を持たず、すべての範囲を読める
- 外部接続用の token に有効期限を設けられるようにした。発行のときに
  30 日、90 日、365 日、または期限なしから選ぶ。選ばなかった場合は 90 日と
  する。期限を過ぎた token では API を使えず、一覧では「期限切れ」として
  並ぶ。この版より前に発行した token は期限を持たず、使い続けられる
- 自分のログイン中の端末を一覧できるようにした。設定の画面から、端末、
  接続元のアドレス、ログインと最後の操作の時刻を確認し、1 件ずつ、または
  この端末以外をまとめて終了できる。終了は監査記録へ残る。期限を過ぎた
  ログインは並ばない
- パスワードの再設定を用意した。ログイン画面から要求すると、登録している
  メールアドレスへ案内が届く。案内は 15 分で使えなくなり、一度使うと
  二度使えない。再設定すると、その利用者の進行中のログインはすべて終わる。
  登録の無いメールアドレスでも応答は同じで、利用者がいるかどうかは
  読み取れない。外部の認証方式へ切り替えている場合、この経路は出さない
- 利用者が自分でパスワードを変更できるようにした。設定の画面から行う。
  変更には今のパスワードが必要で、変更すると他の端末のログインはすべて
  終わる。操作した端末ではそのまま使い続けられる。変更は監査記録へ残る。
  外部の認証方式へ切り替えている場合、この経路は出さない
- 稼働の異常を運用者へ知らせるようにした。`OPERATIONS_EMAIL` を指定した
  場合だけ、1 日 1 回、異常があるときに送る。無事は知らせない。
  知らせるのは診断の失敗と、失敗したジョブや worker の不在といった、
  放っておくと運用が成り立たなくなる注意だけである
- 稼働確認（`/health`）が、ジョブの保存先と添付ファイルの保存先も
  確かめるようになった。どちらかに到達できない場合は 503 を返す。
  `worker` が動いているかは、これまでどおり `/health` では見ない
- 記録を構造化して出せるようにした。`LOG_FORMAT=json` を指定した場合だけ、
  1 行 1 件の JSON で出す。要求と送信については、経路、状態、所要時間、
  利用者の識別子を含む 1 行の要約を足す。指定しない場合の記録は変わらない
- 監査記録を CSV で書き出せるようにした。画面からは一覧の絞り込みを
  反映して書き出す。一度に書き出せるのは 50000 件までで、超える場合は
  切り詰めずに拒否する。全件は
  `bin/rails officeweave:export_audit_events` で標準出力へ書き出せる。
  書き出したこと自体も、件数付きで監査記録へ残る
- 監査記録の保持期間を設けられるようにした。`AUDIT_RETENTION_DAYS` を
  指定した場合だけ、その日数を過ぎた記録を毎日削除する。指定しない場合は
  これまでどおり記録は消えない。現在の設定は監査記録の画面に出る。
  整数でない値や 0 を指定した場合は起動しない
- 取得を時刻起動から無人で呼べるようにした。取得が重なった場合は、
  取得せずに 0 以外で終わる。記録の各行に時刻が付く。cron と
  systemd timer への登録手順を文書へ足した
- 取得した書庫を暗号化できるようにした。`BACKUP_PASSPHRASE_FILE` に
  パスフレーズのファイルを指定した場合だけ、ホストへ届く途中で暗号化する。
  平文の書庫はどこにも作らない。復元でも同じ指定で復号する。指定しない
  場合の振る舞いは変わらない。形式は `openssl enc` の標準の形とし、
  製品が手元に無くても `openssl` だけで中身を取り出せる
- 取得した書庫の数に上限を設けられるようにした。`BACKUP_KEEP` を指定した
  場合だけ、取得の直後に古い書庫を消す。指定しない場合の振る舞いは変わらず、
  書庫は消えない。消すのは取得に成功したあとだけで、対象は
  `script/production_backup` が付けた名前に一致する書庫に限る

### 変更

- 機能ごとの到達度を、機械が読む一覧としても持つようにした。
  `docs/product/capability_registry.yml` が件数、識別子、状態、入口、証拠、
  条件の判定を持ち、文章の正本との食い違いを検査が止める。条件の判定は
  満たす・未達・非該当・未評価の 4 つとし、未評価が残るあいだは complete に
  できない。動作は変わらない
- 配送の規約を検査へ固定した。commit の題名が日本語であること、共同作者の
  付記や生成した道具の宣伝が入らないことを、変更を出すたびに確かめる。
  PR の型と自己レビューの観点も足した。動作は変わらない
- 機能ごとの到達度を文書として持つようにした。`docs/product/` に、機能を
  どの段階へ置くかの決め方、到達度を判定する条件、機能ごとの結果を置く。
  あわせて README の「想定した機能範囲はひととおり実装している」という表現を
  改めた。入口と基礎の実装があることと、受入条件を満たしたこと（実装済み）と、
  版として本番準備済みであることを、それぞれ別の語として扱う。動作は変わらない
- メールの送受信と、利用者が定義を作る業務アプリ基盤を、製品ビジョンの
  非目標から外した。含めないと決めた領域ではなく、採否をまだ決めていない
  領域であるためである。実装の着手条件は変わらず、調査と判断の記録が
  先に必要である。範囲を広げたわけではない
- 必要な Docker の版を Docker Engine 25.0 以降、Docker Compose v2.24 以降と
  した。構成が起動中の確認間隔（`start_interval`）を使うためである。これより
  古い環境では、構成の読み取りの時点で止まる。あわせて、停止した状態から
  すべてのコンテナが健全になるまでの時間が短くなる（手元の実測で約 17 秒
  から約 8 秒）。設定と手順は変わらない
- 配布用の web と worker が 1 つのイメージを共有するようにした。これまでは
  同じ内容のイメージが `officeweave_production-web` と
  `officeweave_production-worker` の 2 つでき、組み立ても 2 回走っていた。
  入れ替え後は `officeweave:production` だけになる。設定、データボリューム、
  実行する手順は変わらない。以前の 2 つは参照されなくなるため、
  `docker image rm officeweave_production-web officeweave_production-worker`
  で取り除ける
- 入口の画面へ並べる申請を、承認待ちと進行中の自分の申請のそれぞれ 5 件までに
  した。並びきらない分へは、追加した「すべての承認待ちを見る」と
  「すべての自分の申請を見る」から移動できる。承認待ちの件数の表示は、
  これまでどおり総数を示す
- ブラウザーへ配信するスクリプトを持たなくなった。全画面から、中身の無い
  スクリプトの読み込みが消える。JavaScript を使う画面はもともと無いため、
  操作と表示は変わらない

### 修正

- 承認の委任で、期間の重なる委任が同時に 2 件成立し得た点を直した。
  データベース側で重なりを拒む。既にある重なりは、移行が和へまとめる。
  その委任で決裁できる日は変わらない
- 部門の階層の循環を、データベース側でも拒むようにした。模型を通らない
  書き込みでも循環を作れなくなる。既にある循環は移行が断ち、断たれた部門は
  根になる
- 予約の重なりの判定を、例外の文面ではなく状態コードと制約の名前で行う
  ようにした。文面はデータベースの版と表示言語で変わるため、変わった日に
  理由を返せなくなっていた
- ログイン、パスワード再設定、外部接続の API の試行の上限を、動かしている
  web の数に依存しないようにした。数え上げをデータベースへ置く。これまでは
  web ごとに数えていたため、台数を増やすと上限がその数だけ増えていた
- 範囲を指定せずに発行した外部接続の token を、発行時点の範囲へ固定した。
  これまでは「すべて」として扱っていたため、資源を足した日に、過去に発行した
  token がその資源へ自動で開いていた。今読める範囲は狭まらない
- 監査の詳細から、本文、コメント、秘密、token を取り除くようにした。
  長すぎる値は切り詰める。監査は誰が何をしたかを追うためのものであり、
  業務の内容の写しではない
- 配布用のイメージの検査で、確認したうえで非該当と判断した指摘を記録して
  通せるようにした。`config/container_scan_exceptions.yml` へ識別子、対象の
  パッケージ、入っている版、根拠、再確認の期限を書く。版が上がるか期限を
  過ぎれば記録は当たらず、検査は再び失敗する。記録した指摘も検査の出力と
  報告に残る。記録の無い指摘は、これまでどおり失敗する
- 承認の段を 2 つ以上持つ種別で、途中の段を承認すると操作が失敗していたのを
  直した。段は進み、履歴も残っているのに画面にはエラーが出る状態だった。
  途中の段の承認も、承認として監査記録へ残る。
  あわせて、決裁の立場を、申請の行を占有したあとの段に対して判定するようにした。
  これまでは占有の前に判定しており、失敗の画面を見て操作をやり直すと、
  1 つの段の担当だけで先の段まで通せた。決裁の画面は、見ていた段を送るように
  なった。別の決裁で先へ進んでいた場合は、状態が変わったことを示して拒む。
  持ち帰るのは署名した値であり、決裁の要求を、その利用者が実際に見た状態に
  対するものだけに限る。差し戻しと再提出で同じ段へ戻った場合も、前の提出の
  ときに開いた画面からは決裁できない。承認済みや差し戻し済みまで進んだあとの
  古い画面も同じ扱いになる。値そのものは記録にも記録の詳細にも残さない。
  あわせて、決裁の立場を確かめるときに、利用者をデータベースから取り直し、
  有効であることと、申請と同じ組織であることも見るようにした。決裁の画面を
  開いたあとに利用を止められた場合や、立場が変わった場合も、その時点の状態で
  判定する。確定したあとの知らせに失敗しても、決裁そのものは成立したままとし、
  失敗は記録へ残す
- 承認の委任で、委任した側を無効にしても代理の権限が残っていたのを直した。
  委任した側が利用できないあいだ、代理での決裁はできず、承認待ちの一覧にも
  並ばない。あらためて利用できるようにし、委任の期間内であれば元に戻る。
  委任した側が担当の部門から外れた場合や、管理者でなくなった場合も同じとする
- 承認の委任で、承認待ちの一覧には出るのに申請を開けなかったのを直した。
  委任を受けた利用者は、一覧から申請を開いて代理で決裁できる。部門を指定しない
  段（管理者が担当する段）も、管理者から委任を受けていれば一覧に出る
- 部門の詳細で、所属が主たるものかどうかを示す欄が空だったのを直した。
  「はい」または「いいえ」が出る。表示だけの不具合であり、どの所属が
  主たるものであるかの記録は変わらない
- 下位部門を持つ部門を削除しようとしたときの理由が、日本語の画面でも
  英語で出ていたのを直した。「下位部門が存在するため削除できません」と
  出る。削除を止める条件は変わらない
- 出来事の送信先へ `#` を含む URL を登録しようとしたときの理由が、
  途中で切れていたのを直した。日本語では「に」だけが出ていた。
  送信先を受け付ける条件は変わらない

## 0.2.0

セキュリティ修正と、送信の永続化を含む。
2026-08-02 より前にこのリポジトリを取得している場合は、この版へ入れ替える。

### セキュリティ

非公開で扱った 4 件の修正。内容は Security Advisory を参照。

- 提出していない申請を、承認担当者と管理者から参照できないようにした。
  一覧にも並ばず、直接の URL でも開けない。申請者は、提出の有無に関わらず
  自分の申請を参照できる。提出したあとは、差し戻しでも取り下げでも
  承認担当者が参照できる状態を維持する
- Webhook の宛先の名前解決を、子プロセスで行うようにした。
  時間切れになった解決が、web と worker のなかに残らなくなった。
  待つ時間の上限、時間切れの理由、解決できない場合の扱いは変わらない
- Webhook を送らない宛先の範囲を広げた。廃止済みの site-local と、
  IPv6 の中へ IPv4 を埋め込む変換や 6to4 の範囲を加えた。
  網の構成によっては内部へ届く経路になり得るものを塞いだ。
  通常の外部の宛先と、許可リストで挙げた origin の扱いは変わらない
- Webhook の応答として受け取る量に、合計 64 KiB の上限を設けた。
  本文だけでなく、ステータス行とヘッダーも同じ上限で扱う。
  圧縮した応答は要求せず、宛先が圧縮して返しても展開しない。
  宛先がどのような応答を返しても、worker が抱える量は変わらなくなった
- Webhook の 1 回の送信に、開始から終了までの上限を 30 秒設けた。
  少しずつ返し続ける宛先が、worker を長く占有できなくなった。
  この上限で打ち切った送信はやり直さない。
  1 回ごとの待ち時間で切れた場合は、これまでどおりやり直す
- Webhook の送信に失敗したときの記録を、決めた文面だけにした。
  接続や TLS の失敗で、宛先や解決した IP が記録へ残らなくなった。
  やり直すかどうかの判断は変わらない

### 変更

- メールと Webhook の送信を永続化した。web と worker を入れ替えても、
  未処理の送信が失われなくなった
- 送信を実行する `worker` を追加した。ホストへポートを公開しない
- 未処理のジョブを、業務データとは別のデータベースへ置くようにした。
  接続先の PostgreSQL は同じで、新しいコンテナや公開ポートは増えない
- 一時的な失敗を最大 5 回までやり直すようにした。
  恒久的な失敗はやり直さず、記録として残す
- バックアップの対象へ未処理のジョブを加えた。書庫の形式を 2 とした
- 取得と復元では web と worker の両方を停止するようにした
- ログイン状態に無操作 30 分と、開始から 8 時間の期限を設けた。
  どちらかを超えると、あらためてログインが必要になる
- 認証後の戻り先を、アプリケーション内の経路だけに限った。
  言語の切り替え後の戻り先にも同じ制限を適用した
- 運用環境で、`APPLICATION_HOST` と異なるホスト名の要求を拒否するようにした。
  稼働確認の経路も同じ扱いとする
- `APPLICATION_HOST` の誤設定を起動時に拒否するようにした。
  スキーム、経路、ポート、前後の空白を含む値では起動しない。
  IP アドレスとして成立しない値と、ゾーン識別子を含む IPv6 も拒否する
- 利用者が 80 または 443 以外のポートへ接続する場合に、
  公開 URL へ付けるポートを `APPLICATION_PORT` で指定できるようにした。
  ホストへ公開する `WEB_PORT` とは別に指定する
- `bin/diagnose` で、公開 URL が利用者の端末自身を指す場合と、
  接続先を特定しないアドレスの場合に注意を出すようにした
- 添付ファイルの取得を文書の配下の経路だけに限った。
  保存基盤が用意する `/rails/active_storage/` の経路は全環境で作らない。
  署名付きの URL を知っていても、文書を参照できない利用者は取得できない
- 利用者と部門の CSV を、表計算ソフトで開いたときに
  入力された値が数式として解釈されないよう保護して書き出すようにした
- OfficeWeave が書き出した利用者 CSV を取り込むと、
  保護のために付けた文字を取り除いて元の値へ戻すようにした
- 同じ申請へ承認、差し戻し、取り下げが同時に届いた場合に、
  状態の変更、履歴、通知が重複しないようにした。
  先に成立した処理だけが記録され、遅れた処理は理由を示して失敗する
- 新しく設定するパスワードを 15 文字以上とした。
  文字数で数え、大文字・数字・記号の混在は求めない
- 空白だけで構成されたパスワードを拒否するようにした。
  タブと全角空白も同じ扱いとする。空白を含む通常の値は受理する
- `change_me`、`password`、`officeweave` を、
  前後の空白と大文字小文字の違いを問わず拒否するようにした
- 初期利用者の資格情報から既定値をなくした。
  `INITIAL_USER_EMAIL` と `INITIAL_USER_PASSWORD` を設定するまで、
  初期利用者は作成しない。開発環境も同じとする
- 初期管理者の資格情報を、稼働し続ける web と worker へ渡さないようにした。
  `script/seed_initial_user` が作る一時コンテナだけで使用する。
  作成後は `.env` から削除でき、web と worker の作り直しは要らない
- 初期利用者を作成するコマンドを `script/seed_initial_user` にした。
  配布用の構成では `--production` を付ける
- `bin/diagnose` で、`INITIAL_USER_PASSWORD` が Rails の実行環境へ
  渡っている場合に注意を出すようにした。値の強弱は問わない
- 初期利用者の資格情報を `.env` または `--env-file` からだけ読むようにした。
  同名のホスト環境変数は使わない
- `.env.example` の秘密情報を空欄とし、`change_me` を残さないようにした
- 初期資格情報の危険な設定を `bin/diagnose` で確認できるようにした。
  設定に残った既知の初期値と、その値をそのまま使っている利用中の管理者を知らせる
- 利用者を無効にすると、発行済みの token も失効するようにした。
  再び有効にしても、失効した token は元に戻らない。必要な場合は発行し直す
- 無効にした利用者へ token を発行できないようにした。
  無効化と発行が同時に実行されても、有効な token は残らない
- パスワードを変更すると、その利用者の進行中のログイン状態が終わるようにした。
  変更前のログイン状態は期限を待たずに使えなくなる。
  氏名や表示言語だけの更新では、ログイン状態は続く
- 予約に結び付ける予定を、予約者が参照できるものだけに限った。
  画面の選択肢に無い予定の id を送っても保存されない。
  存在しない id を送った場合も、応答が失敗せず理由が表示される。
  別組織の予定、参照できない予定、存在しない予定は、いずれも同じ理由になる
- 出来事の送信先の変更を、監査記録へ残すようにした。
  変更前と変更後の URL、および有効状態が残る。
  値を変えずに保存した場合は記録が増えない。`secret` は残さない
- 本文へ入力した記号を、記号のまま表示するようにした。
  これまでは一部のタグが要素として解釈され、任意のリンクと
  外部の画像を埋め込めた。改行と空行の見え方は変わらない。
  文書、お知らせ、予定、申請、申請の履歴、設備・備品が対象
- 予約に結び付ける予定と、予約者が、予約と同じ組織に属することを
  求めるようにした。画面の選択肢に無い別組織の予定の id を送っても保存されない
- 作成者、持ち主、申請者、実行者、通知の宛先、公開先の部門についても、
  同じ組織に属することを求めるようにした。組織で絞った一覧、集計、通知が
  組織の境界を越えなくなった
- 自分の設定の更新で、表示言語と通知の受け取り設定をまとめて確定する
  ようにした。どちらかが保存できない場合は両方とも変更されず、
  理由が画面に表示される
- 予定の API の `from` に解析できない値を指定した場合に、400 と
  不正なパラメーター名を返すようにした。現在時刻へ読み替えない。
  未指定の場合は従来どおり現在時刻以降を返す
- 予定と予約の一覧で、表示開始日に扱える範囲を超えた年を指定した場合も
  一覧へ戻すようにした。これまでは応答が失敗していた
- 初期利用者の作成で、利用者の有無を組織ごとに見るようにした。
  `ORGANIZATION_CODE` を変えて実行すると、その組織の初期管理者が作られる。
  これまでは他の組織に利用者がいると作られなかった
- 公開日時を先に設定したお知らせを、管理者の一覧へ「公開待ち」として
  表示するようにした。下書き・公開待ち・公開済みの 3 区分になった
- 公開日時が来た時点で、対象の利用者へ知らせるようにした。
  同じお知らせについて知らせは 1 回だけ送る。
  一般利用者には、公開日時が来るまで表示されない
- お知らせの一覧で、公開待ちと下書きの作成者もまとめて読むようにした。
  一覧を開くのにかかる時間が、公開待ちと下書きの件数で増えなくなった。
  表示される内容と並び順は変わらない
- ログイン状態の最終利用日時を、要求のたびではなく一定の間隔で
  記録するようにした。画面を開くだけの操作が書き込みを伴わなくなった。
  無操作 30 分と開始から 8 時間の期限は変わらない。
  記録は遅れる向きにしか働かないため、期限が延びることはない
- API トークンの最終利用日時も同じ間隔で記録するようにした
- 入口の画面で、未読の判定を並べるお知らせの分だけ行うようにした。
  入口を開くのにかかる時間が、蓄積したお知らせの件数で増えなくなった。
  未読の印と件数の表示は変わらない
- 利用者の一覧で、主たる所属をまとめて読むようにした。
  一覧を開くのにかかる時間が、利用者の人数で増えなくなった。
  表示される所属は変わらない
- 部門の階層の表示で、上位の部門をまとめて読むようにした。
  部門の一覧や公開範囲の選択肢を開くのにかかる時間が、
  部門の件数と階層の深さで増えなくなった。表示される階層は変わらない
- 複数の受け手への通知を、まとめて作るようにした。
  お知らせの公開にかかる時間が、受け手の人数で増えなくなった。
  メールの送信は、受け手ごとの積み込みをワーカーが行う。
  届く通知とメールの内容は変わらない
- 配布用の構成が必須とする `SECRET_KEY_BASE` を `.env.example` へ加えた。
  `.env.example` を複製しただけでは配布用の構成が起動しない状態を解消した
- `SMTP_DOMAIN` を配布用の構成から web と worker へ渡すようにした。
  `.env` へ設定しても送信時のドメインへ反映されない状態を解消した

### 補足

- 送信の実行は at-least-once である。同じ通知が二度届くことがある
- Webhook には `X-OfficeWeave-Delivery-Id` を添える。
  受け取る側は、この値で重複を判別できる
- 形式 2 より前の書庫も復元できる。ただし未処理のジョブは復元されない
- セッションの期限は現段階では設定で変更できない
- パスワードの変更では API token を失効させない。
  token はパスワードから導かれる資格情報ではなく、用途ごとに発行して
  個別に失効できる。利用そのものを止める場合は、利用者を無効にする
- 更新の適用時、既存のログイン状態には最終利用日時の記録がない。
  移行では最後に更新された時刻を無操作期限の起点として扱うため、
  適用直後にあらためてログインを求められる利用者がいる。
  残りのログイン状態も、開始から 8 時間で順次終わる
- 稼働確認をループバックの IP へ直接送っている場合は、
  `Host` ヘッダーへ `APPLICATION_HOST` の値を付ける必要がある
- `APPLICATION_HOST` を未設定にした場合と、`APPLICATION_HOST=` と
  空欄にした場合は異なる。空欄では起動しない
- これまで `APPLICATION_HOST` へポートを含めていた場合は、
  ホスト名を `APPLICATION_HOST`、ポートを `APPLICATION_PORT` へ分ける
- 利用者が 443 へ接続する場合、`APPLICATION_PORT` は設定しない。
  `WEB_PORT` を公開 URL へ流用しないため、メール本文へ内部のポートは入らない
- CSV の保護は、最初に開いたときに数式として評価させないための措置である。
  表計算ソフトで編集して保存し直した CSV は、ソフト側の変換を受ける
- パスワードの要件は、新しく設定する値にだけ適用する。保存済みのパスワードは
  自動では無効にしない。要件を満たさない値で運用している利用者も、
  これまでどおりログインできる。設定し直す場合にだけ要件を満たす値が必要になる
- `bin/diagnose` が見えるのは Rails の実行環境だけである。ホストの `.env` に
  `INITIAL_USER_PASSWORD` が残っているかどうかは判定できない。
  作成後にファイルから削除したことは、ファイル側で確かめる
- `bin/diagnose` が見つけられるのは、Rails の実行環境に残った既知の初期値と、
  保存済みの管理者が既知の初期値そのものを使っている状態だけである。
  保存済みの digest からは復元できないため、弱いパスワード全般は判定できない
- これまで開発環境の既定の資格情報でログインしていた場合、
  `.env` へ `INITIAL_USER_EMAIL` と `INITIAL_USER_PASSWORD` を設定する必要がある。
  既に作成済みの利用者はそのまま使える

## 0.1.0

最初の公開。組織の日常業務を一巡できる範囲を実装した。

### 追加

- 組織、部門、所属、利用者、権限
- ログインとログアウト、利用者の無効化
- 日本語と英語の表示、利用者ごとの言語設定
- お知らせ、公開範囲、既読
- 個人予定と共有予定
- 設備・備品と、時間帯が重ならない予約
- 申請、承認、差し戻し、操作の履歴
- 文書、分類、添付ファイル、参照範囲、全文検索
- アプリ内通知とメール通知、種類ごとの配信設定
- 参照用の REST API、出来事の送信、外部認証の差し替え口
- 利用者と部門の CSV 入出力
- 監査記録、バックアップと復元、運用診断
- 導入と運用の手順、動作確認用のデータ
- セルフホスト向けの配布構成

### 制限

- 承認は 1 段階のみ。多段の承認経路は扱わない
- 予約の変更は、取り消して取り直す
- 部門の CSV 取り込みは扱わない（書き出しのみ）
- 出来事の送信に失敗した場合の再試行は行わない
- 単一ホストでの運用を前提とする
