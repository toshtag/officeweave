<% content_for :title, t("reservations.edit.title") %>

<div class="panel panel--narrow stack">
  <h1><%= t("reservations.edit.heading") %></h1>
  <%= render "form", reservation: @reservation %>
  <%= link_to t("common.back"), reservations_path %>
</div>
