<% content_for :title, t("request_decisions.conflict.title") %>

<div class="panel panel--narrow stack">
  <h1><%= t("request_decisions.conflict.heading") %></h1>
  <p><%= t("request_decisions.conflict.description") %></p>
  <p><%= link_to t("request_decisions.conflict.back"), request_path(@request) %></p>
</div>
