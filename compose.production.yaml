# 配布用の構成。
#
# 開発用（compose.yaml）とは別に用意する。
# 開発用はソースコードをホストから共有するため、配布には使えない。
#
# project 名とデータボリュームは開発用と分ける。
# 同じ名前を使うと、開発用の `docker compose down -v` が
# ここで運用しているデータベースを削除し得る。
#
# この構成を扱うコマンドには、必ず -f compose.production.yaml を付ける。
# 付け忘れると開発用の構成に対して実行される。
#
# 使い方:
#   docker compose -f compose.production.yaml up -d --build
name: officeweave_production

# web と worker が動かすイメージ。
#
# 名前を与えないと、compose は service ごとに別のイメージを作る。中身は同じ
# であり、一覧には同じものが 2 行並び、組み立ても 2 回走る。
#
# 開発用（compose.yaml）と違い、project 名からは作らない。配布先で動くのは
# この構成 1 つであり、並行する別の project と分ける必要が無い。
x-application-image: &application_image officeweave:production

# web と worker は同じ設定で動く。
# 片方だけへ設定を足すと、画面では通るのに送信だけが失敗する状態が生まれる。
x-application-environment: &application_environment
  RAILS_ENV: production
  DATABASE_HOST: db
  DATABASE_USER: ${DATABASE_USER:?DATABASE_USER を設定してください}
  DATABASE_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD を設定してください}
  DATABASE_NAME: ${DATABASE_NAME:-officeweave_production}
  QUEUE_DATABASE_NAME: ${QUEUE_DATABASE_NAME:-officeweave_production_queue}
  QUEUE_DATABASE_CONNECTIONS: ${QUEUE_DATABASE_CONNECTIONS:-5}
  JOB_THREADS: ${JOB_THREADS:-2}
  JOB_PROCESSES: ${JOB_PROCESSES:-1}
  SECRET_KEY_BASE: ${SECRET_KEY_BASE:?SECRET_KEY_BASE を設定してください}
  # 未設定の場合だけ localhost を使う。
  # 空文字は既定値へ置き換えず、そのまま渡す。設定の誤りは起動時に拒否する。
  APPLICATION_HOST: ${APPLICATION_HOST-localhost}
  APPLICATION_PROTOCOL: ${APPLICATION_PROTOCOL:-https}
  # 利用者が接続する公開ポート。逆プロキシで 80 または 443 を公開する構成では指定しない。
  # 下の WEB_PORT とは別物であり、値を流用しない。
  APPLICATION_PORT: ${APPLICATION_PORT-}
  MAIL_FROM: ${MAIL_FROM:-officeweave@localhost}
  SMTP_ADDRESS: ${SMTP_ADDRESS:-}
  SMTP_PORT: ${SMTP_PORT:-587}
  # 送信時に名乗るドメイン。設定側では受け付けていたが、ここで渡していなかった。
  SMTP_DOMAIN: ${SMTP_DOMAIN:-}
  SMTP_USER_NAME: ${SMTP_USER_NAME:-}
  SMTP_PASSWORD: ${SMTP_PASSWORD:-}
  SMTP_AUTHENTICATION: ${SMTP_AUTHENTICATION:-}
  SMTP_ENABLE_STARTTLS: ${SMTP_ENABLE_STARTTLS:-true}
  # 初期利用者と組織の設定（INITIAL_USER_*、ORGANIZATION_*）はここへ置かない。
  # 使うのは bin/rails db:seed だけで、web と worker の通常の実行には要らない。
  # 常駐するコンテナへ渡すと、初期管理者の平文パスワードがその寿命のあいだ
  # 環境に残り続ける。.env から消しても、作成済みのコンテナからは消えない。
  # script/seed_initial_user が、一時コンテナへ変数名だけを指定して渡す。
  #
  # 未設定の場合だけ内部認証を使う。
  # 空文字は既定値へ置き換えず、そのまま渡す。設定の誤りは起動時に拒否する。
  AUTHENTICATION_PROVIDER: ${AUTHENTICATION_PROVIDER-internal}
  WEBHOOK_PRIVATE_DESTINATION_ALLOWLIST: ${WEBHOOK_PRIVATE_DESTINATION_ALLOWLIST:-}
  FORCE_SSL: ${FORCE_SSL:-true}
  # 準備は prepare が一度きりで行う。web の起動処理では行わない。
  DATABASE_PREPARED_ELSEWHERE: "1"
  # ホストから web コンテナへ公開するポート。
  # メール本文の公開 URL へは使わない。逆プロキシの背後では、
  # 利用者が接続するポートと一致しない。公開 URL は APPLICATION_PORT で指定する。
  WEB_PORT: ${WEB_PORT:-3210}

services:
  db:
    image: postgres:18.4-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DATABASE_USER:?DATABASE_USER を設定してください}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD を設定してください}
      POSTGRES_DB: ${DATABASE_NAME:-officeweave_production}
    volumes:
      - production_db_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER}"]
      interval: 10s
      timeout: 5s
      retries: 20
      # 起動のあいだだけ細かく見る。稼働中の判定は 10 秒ごとで足りるが、
      # その間隔をそのまま起動へ使うと、準備が終わっていても次の確認まで待つ。
      # 後ろに web と worker が続くため、この待ちは 3 段ぶん積み上がる。
      start_period: 30s
      start_interval: 1s

  # データベースの準備。
  #
  # 移行を web の起動処理の中で行うと、準備の途中は 3000 番へ何も応えない。
  # 稼働確認はそれを「応えない」としか見られず、移行が長引いた分だけ失敗が
  # 積み上がって unhealthy になる。web を待つ worker は、そこで起動を諦める。
  #
  # 準備を一度きりの実行として切り出す。完了を待つ依存には、稼働確認のような
  # 打ち切りが無い。移行がどれだけ長引いても、待つ側は待ち切る。
  #
  # web と worker の両方がこれを待つ。同時に移行を実行して競合させない。
  prepare:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: *application_image
    command: bin/rails db:prepare
    environment:
      <<: *application_environment
      # ここが準備そのものである。起動処理の分岐は通らないが、明示する。
      DATABASE_PREPARED_ELSEWHERE: "0"
    depends_on:
      db:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: *application_image
    restart: unless-stopped
    environment:
      <<: *application_environment
    volumes:
      # アップロードされたファイルは、入れ替えても失われないようにする。
      - production_storage_data:/app/storage
    healthcheck:
      # データベースの準備は web の起動処理が行う。
      # worker はその完了を待つため、web の稼働をここで判定できるようにする。
      #
      # 運用環境は APPLICATION_HOST 以外の Host を拒否する。
      # ループバックへ直接要求するため、Host は明示する。
      # $$ で escape し、compose ではなくコンテナ内の shell へ展開させる。
      # web へ実際に渡っている値と、確認に使う値を必ず一致させる。
      # ここで既定値へ落とさない。落とすと、誤設定でも稼働確認だけが通る。
      test: ["CMD-SHELL", "curl -fsS -H \"Host: $${APPLICATION_HOST}\" http://127.0.0.1:3000/up >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s
      start_interval: 1s
    ports:
      # 既定では loopback にだけ公開する。逆プロキシの背後へ置く構成を前提とする。
      # 逆プロキシを使わず外部へ直接公開する場合に限り、
      # WEB_BIND_ADDRESS=0.0.0.0 を利用者が明示する。
      - "${WEB_BIND_ADDRESS:-127.0.0.1}:${WEB_PORT:-3210}:3000"
    depends_on:
      db:
        condition: service_healthy
      prepare:
        condition: service_completed_successfully

  # メールと Webhook の送信を実際に行う。
  #
  # 画面を提供しないため、ホストへポートを公開しない。
  #
  # web ではなく準備の完了を待つ。worker に要るのは「移行が済んでいること」
  # であり、「web が応えていること」ではない。web の稼働確認を待たせると、
  # 準備が長引いた回だけ worker が起動しない。
  worker:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: *application_image
    command: bin/jobs
    restart: unless-stopped
    # 実行中のジョブを終わらせる時間を与える。
    # 足りないと、処理の途中で止まったジョブがやり直しになる。
    stop_grace_period: 30s
    environment:
      <<: *application_environment
    volumes:
      - production_storage_data:/app/storage
    healthcheck:
      test: ["CMD-SHELL", "bin/jobs_alive"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s
      start_interval: 1s
    depends_on:
      db:
        condition: service_healthy
      prepare:
        condition: service_completed_successfully

volumes:
  # 開発用（compose.yaml）は、project 名にある語をキーから外して短くした。
  # ここでは行わない。キーを変えると実際の名前が変わり、既に運用している
  # 内容が参照されなくなる。入れ替えの時点で、空のボリュームから起動する。
  # 一覧の読みやすさのために払う代償ではない。
  production_db_data:
  production_storage_data:
