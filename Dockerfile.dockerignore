# 開発用の組み立てへ渡すもの。
#
# BuildKit は、Dockerfile と同じ名前に .dockerignore を付けたファイルがあれば、
# その組み立てにだけ適用する。配布用（.dockerignore）とは別に指定できる。
#
# 開発用のイメージはソースを写さない。実行時は compose がホストの内容を
# /app へ重ねる。渡す必要があるのは、依存を解決するための 2 つだけである。
#
# それ以外を渡すと、使いもしないものが組み立てのたびに読み取られ、
# 組み立ての記録へ写しが残る。
*
!Gemfile
!Gemfile.lock
